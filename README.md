This is a React project, built with Vite and using Tailwind.

## Available Script

In the project directory, you can run:

### `npm run dev`

Runs the app in the development mode.<br />
Open [http://localhost:5173/react/](http://localhost:5173/react/) to view it in the browser.

The page will reload if you make edits.<br />
